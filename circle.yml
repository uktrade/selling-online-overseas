machine:
  node:
    version: 6.10.3
  python:
    version: 3.5.0
  java:
    version: oraclejdk8

dependencies:
  pre:
    - echo "NAV_BUILD_TYPE=local"> build.env
    - cp scripts/local/test.env scripts/local/vars.env
  override:
    - make

test:
  override:
    - make test
